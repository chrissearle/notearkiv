= simple_form_for @genre, :html => { :class => 'form-horizontal' } do |f|
  %fieldset
    %legend= t('model.genre.title')

    = f.input :name

    %div.form-actions
      = f.button :submit, :class => 'btn-primary'
      = link_to t('form.button.cancel'), genres_path, :class => 'btn'
