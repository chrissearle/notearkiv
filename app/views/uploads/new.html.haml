= simple_form_for @upload, :html => {:class => 'form-horizontal'} do |f|
  %fieldset
    %legend= t('upload.form.legend')

    = f.input :title

    %div.form-actions
      = f.button :submit, :class => 'btn-primary'
