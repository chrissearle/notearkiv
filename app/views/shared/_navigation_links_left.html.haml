%ul.nav.navbar-nav
  - if current_user
    - if permitted_to? :index, :notes
      %li= link_to t('menu.notes.title'), typedsearch_path(:type => 'note')
    - if permitted_to? :index, :evensongs
      %li= link_to t('menu.evensongs.title'), typedsearch_path(:type => 'evensong')
