= simple_form_for @period, :html => { :class => 'form-horizontal' } do |f|
  %fieldset
    %legend= t('model.period.title')

    = f.input :name

    %div.form-actions
      = f.button :submit, :class => 'btn-primary'
      = link_to t("form.button.cancel"), periods_path, :class => 'btn'
