- if permitted_to? :new, :notes
  %div.well
    = link_to t('list.button.new'), new_note_path, :class => 'btn btn-primary'
    = link_to link_with_icon("icon-download", t('list.button.download')), notes_path(:format => :xls), :class => 'btn'
