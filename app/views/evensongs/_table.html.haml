%table.table.table-bordered.table-condensed.table-striped
  %thead
    %tr
      %th= t('table.title.title')
      %th= t('table.title.psalm')
      %th= t('table.title.composer')
      %th= t('table.title.genre')
      %th= t('table.title.upload')
  %tbody
    - rows.each do |item|
      %tr
        %td
          - if permitted_to? :show, item
            = link_to item.title, evensong_path(item)
          - else
            = item.title
        %td= typed_search_link 'search', item.psalm
        %td= typed_search_link 'composer', item.composer_name
        %td= typed_search_link 'genre', item.genre_name
        %td
